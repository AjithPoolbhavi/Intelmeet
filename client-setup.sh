# This would be run: npm create vite@latest client -- --template react-ts
# Pre-creating the structure instead
mkdir -p client/src/{components/{ui,meeting,dashboard,auth,tasks},pages,hooks,store,services,types}
echo "Client structure created"
